name: cls_digits_sensitivity
dataset: digits
seed: 42

model:
  hidden_dim: 128
  activation: tanh

train:
  batch_size: 32
  lr: 0.01
  weight_decay: 0.0005
  early_stopping_patience: 15

al:
  strategy: sensitivity
  init_frac: 0.1
  query_size: 10
  budget: 200
