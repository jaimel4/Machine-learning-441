name: reg_diabetes_uncertainty
dataset: diabetes
seed: 42
model: { hidden_dim: 32, activation: relu }
train: { batch_size: 32, lr: 0.0005, weight_decay: 0.001, early_stopping_patience: 30 }
al: { strategy: uncertainty, init_frac: 0.1, query_size: 5, budget: 150, mc_dropout_passes: 10 }
