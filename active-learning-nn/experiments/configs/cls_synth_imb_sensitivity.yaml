name: cls_synth_imb_sensitivity
dataset: clf_synth
seed: 42
model:
  hidden_dim: 128
  activation: tanh
train:
  batch_size: 32
  lr: 0.01
  weight_decay: 0.0005
  early_stopping_patience: 15
al:
  strategy: sensitivity
  init_frac: 0.1
  query_size: 10
  budget: 300
